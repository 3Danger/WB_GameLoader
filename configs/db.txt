https://habr.com/ru/post/254425/


CREATE TABLE users
(
    login VARCHAR UNIQUE NOT NULL PRIMARY KEY,
    password VARCHAR NOT NULL,
    isCustomer BOOLEAN NOT NULL,
    data JSONB NOT NULL
);


INSERT INTO users (login, password, iscustomer, data)
VALUES ('csamuro', 'qwe', true, '{"money": 3.1415928}');



SELECT * FROM users
WHERE data @> '{"money":4}';

... WHERE data ->> 'money' > '31';


CREATE TABLE tasks
(
    id SERIAL PRIMARY KEY NOT NULL UNIQUE,
    data JSONB
)


